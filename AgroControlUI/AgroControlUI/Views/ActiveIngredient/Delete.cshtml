@model AgroControlUI.DTOs.CropProtectionProducts.ActiveIngredientDto

@{
    ViewData["Title"] = "Usuń składnik aktywny";
}

<h1>Usuń składnik aktywny</h1>

<div class="alert alert-warning">
    <strong>Uwaga!</strong> Czy na pewno chcesz usunąć ten składnik aktywny?
</div>

<div>
    <p><strong>Nazwa:</strong> @Model.Name</p>
</div>

<form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Usuń</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Anuluj</a>
</form>
